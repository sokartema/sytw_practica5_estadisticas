.row
    .col-md-5.container

        %h1 Bienvenido Anónimo


.row
    .col-md-4.container.jumbotron
        %h2 Acorta tu URL!!
        %form{:name => "input" , :action=>"/" , :method=>"post"}
            %label{:for=>"url"} URL:
            %input.form-control#url{:type=>"text", :name=>"url" ,:placeholder=>"http://ejemplo.com"}
            %button.btn.btn-success{:type=>"submit"} Entrar
            -if(session[:url] != nil)
                %ul
                    -@list2.each do |url|
                        %li
                            #{url.url}
                            %a{:href=>"/#{url.id.to_s(Base)}"} (#{url.id.to_s(Base)})


    .col-md-1
    .col-md-7.container.jumbotron
        %h2 URLs
        %ul
            -@list.each do |url|
                %li
                    #{url.url}
                    %a{:href=>"/#{url.id.to_s(Base)}"} (#{url.id.to_s(Base)})
        %form{:name=> "input" , :action => "/auth/google_oauth2" , :method=>"post"}
            %button.btn.btn-danger{:id=>"googleboton",:type => "submit"} Sign In With Google+
        %form{:name=> "input" , :action=>"/auth/twitter",:method=>"post"}
            %button.btn.btn-info{:id=>"twitterboton",:type =>"submit"} Sign In With Twitter
        %form{:name=> "input" , :action=>"/auth/facebook",:method=>"post"}
            %button.btn.btn-facebook{:id=>"facebookboton",:type =>"submit"} Sign In With Facebook

    .col-md-1
