.row
    .col-md-5.container

        %h1 Bienvenido, Anónimo


.row
    .col-md-5.container.jumbotron
        %h2 Acorta tu URL!!
        %form{:name => "input" , :action=>"/" , :method=>"post"}
            %label{:for=>"url"} URL:
            %input.form-control#url{:type=>"text", :name=>"url" ,:placeholder=>"http://ejemplo.com"}
            %button.btn.btn-success{:type=>"submit"} Entrar
            -if(session[:url] != nil)
                %ul
                    -@list2.each do |url|
                        %li
                            #{url.url}
                            %a{:href=>"/#{url.id.to_s(Base)}"} (#{url.id.to_s(Base)})
                            %a.text-primary{:href=>"/estadisticas/#{url.id.to_s(Base)}"}
                                %img{:src=>"icons/stats.png"}



    .col-md-1
    .col-md-5.container.jumbotron
        %h2 URLs
        %ul
            -@list.each do |url|
                %li
                    #{url.url}
                    %a{:href=>"/#{url.id.to_s(Base)}"} (#{url.id.to_s(Base)})
                    %a.text-primary{:href=>"/estadisticas/#{url.id.to_s(Base)}"}
                        %img{:src=>"icons/stats.png"}

        %form{:name=> "input" , :action => "/auth/google_oauth2" , :method=>"post"}
            %button.btn.btn-danger{:id=>"google-button",:type => "submit"} Sign In With Google+
        %form{:name=> "input" , :action=>"/auth/twitter",:method=>"post"}
            %button.btn.btn-info{:id=>"twitter-button",:type =>"submit"} Sign In With Twitter
        %form{:name=> "input" , :action=>"/auth/facebook",:method=>"post"}
            %button.btn.btn-facebook{:id=>"facebook-button",:type =>"submit"} Sign In With Facebook

    .col-md-1
